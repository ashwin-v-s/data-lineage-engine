# ingestion/sql (M1)
SQLGlot adapter implementing contracts.interfaces.StaticLineageProvider.
First tasks: corpus entries with hand-derived expected dependencies (projection, alias, WHERE, CASE, INNER JOIN), then extract(). Pack Section 30.
