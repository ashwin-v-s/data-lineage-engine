# research/ground_truth (M2)
GroundTruthProvider implementations (ProvSQL, ReferenceInterpreter) and the spike report.
Independence rule: must not import ingestion/, sqlglot, research.reasoning or research.baselines (checked in CI).
Pack Sections 13 and 31.
